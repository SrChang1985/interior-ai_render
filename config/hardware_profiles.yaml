cat > config/hardware_profiles.yaml << 'EOF'
# Este archivo se genera automáticamente durante el setup
# Ejecuta: python -c "from core.hardware_detector import HardwareDetector; d = HardwareDetector(); d.save_profile()"

# Placeholder - será sobrescrito
placeholder: true
EOF
